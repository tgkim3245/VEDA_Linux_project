#include "lib_buzzer.h"

void buzzerControl(){
    // printf("buzzerControl 라이브러리 동작함\n");
    
    // led_init();
    // while(!(((struct st_led_data*)arg)->kill_thread)){
    //     int sd = ((struct st_led_data*)arg)->sd;
    //     int brightness = ((struct st_led_data*)arg)->brightness;
    //     led_pwm('r', brightness);
    //     delay(100);
    // }
    // ((struct st_led_data*)arg)->kill_thread = 0;
    // return NULL;
}